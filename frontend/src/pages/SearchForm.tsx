import { Card } from "../components/Card";
import { useNavigate } from "react-router-dom";
export function SearchForm(){
    return(
        <Card title="This is for debugging.">
            <p>Hey there! what are you doing.</p>
        </Card>
    );
}